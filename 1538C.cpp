#include <bits/stdc++.h>
using namespace std;
int rangecount(vector<int> vec,int left, int r){
    sort(vec.begin(),vec.end());
    int l=0;
    int r= vec.size()-1;
    while(l<r){
        if()
    }
}
int main(){
    int n;
    cin >> n;
    for(int i=0;i<n;i++){

    }
}